<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e885d-0f18-76fa-adda-9c4ddbe648b2 -->
* **ZMK Corne base layer layout: leftmost column and thumb cluster assignments**: ZMK Corne keymap: custom \`lt\_tog\` hold-tap for layer thumb keys. RAISE layer (3): top row = \`\` \` 1–0 - \`\`; right home row = \`HOME UP END \ . .\`; right bottom row = \`PGUP LFT DWN RGT PGDN .\`; left rows = media/volume. ADJUST layer (2, repurposed LOWER slot): F1–F12 (F12 top-left), BT/RGB/layout-switch/bootloader (BOOT top-right). Left-inner thumb: \`\&lt\_tog ADJUST ADJUST\`. Right thumb (inner→middle→outer): \`\&ht RCTRL RET\` / \`\&kp LGUI\` / \`\&lt\_tog RAISE RAISE\`. Mid-right: \`\&ht RSHFT SQT\` (tap \`'\` / hold Right Shift). Bottom-right: \`\&ht LC(LALT) RBKT\` (tap \`]\` / hold Ctrl+Alt). Top-left: plain \`\&kp TAB\`. Top-right: plain \`\&kp ESC\`. Word movement: hold \`\&mt LCTRL DEL\` + RAISE simultaneously (Ctrl+arrows on RAISE layer).

### Gotcha

<!-- lore:019f5136-62ff-7b81-a0e6-9e1551f7d7e0 -->
* **ZMK Corne boot loop after flashing new firmware: check stale BLE bonds**: When a ZMK Corne (Corne Min) keyboard boot-loops after flashing new firmware, suspect stale settings/BLE bonds left in flash from the previous firmware rather than a build/flash failure — CI only validates that the firmware builds, not runtime behavior against existing bonded state on the device. Trap: assuming the new firmware image itself is broken and re-flashing repeatedly looks like the fix, but the loop can persist because old bond/settings data in flash is incompatible with the new build. Fix: clear settings/BLE bonds (e.g. via bootloader double-tap + settings reset, or clearing the settings partition) before/after flashing when boot loops appear post-flash. Unconfirmed hypothesis as of Jul 11 2026 — verify against actual resolution once confirmed.

### Pattern

<!-- lore:019e885d-0f0b-7557-9359-7bb6190da67d -->
* **ZMK Corne keymap: custom lt\_tog hold-tap for layer thumb keys**: ZMK Corne base layer layout: custom lt\_tog hold-tap for layer thumb keys. The user wants to allow timing tweaks with special keys in adjust layer, but it's not possible without patching ZMK's C source. An alternative is compiling multiple presets and switching between them live with keys.

### Preference

<!-- lore:019f6197-dfbf-7aa7-9a1b-a4e1dfc02cfa -->
* **prefers quick tap for ' and slow press for Shift**: The user prefers ' to appear with quick press and Shift with deliberate slow press. This behavior is set for the Space key and other keys.

<!-- lore:019f6197-c9ab-7332-9ac9-a3a9698b3c6d -->
* **Always types unless deliberately held**: Always types unless deliberately held. The user also prefers this behavior for other keys, with specific settings for ' and Shift keys.

<!-- lore:019f5e49-53d5-7157-a41f-17d96d7bcc82 -->
* **Directive: trigger ADJUST layer via tap-toggle, not conditional layers**: Directive: trigger ADJUST layer via tap-toggle (e.g., \`\&lt\_tog ADJUST ADJUST\`) rather than conditional layers. This approach is preferred for reliability and consistency across keyboard layers. The user also prefers to allow timing tweaks with special keys in the adjust layer, but notes that ZMK limitations make it not possible without patching ZMK's C source.

<!-- lore:019f6197-c9c4-70f3-b8be-e9b81b16951e -->
* **Never slips out a stray tap (e**: Never slips out a stray tap (e.g., Tab). The user's keyboard behavior is set to prevent accidental taps.

<!-- lore:019f5e49-5410-77bb-a342-3caf7b77a0e9 -->
* **Prefer tap-preferred mod-tap behavior for Space key**: Prefer tap-preferred mod-tap behavior for Space key: tapping Space followed by another key doesn't trigger the hold action unexpectedly. The user prefers ' to appear with quick press and Shift with deliberate slow press.

<!-- lore:019f5e49-5445-73e3-8d3e-be880519126c -->
* **Use custom hold-tap behaviors for specific keys**: Use custom hold-tap behaviors (e.g., \`\&hs\` for Shift keys) to fine-tune timing and flavors for specific keys. This approach allows for more precise control over key behavior and better handling of fast key combos.

<!-- lore:019e854a-623a-77b9-a5e1-a672f1cd025f -->
* **zmk-config-corneview: commit directly to main (user-directed)**: For the zmk-config-corneview repo, Burak Yigit Kaya explicitly directs committing and pushing directly to \`main\` — no PR/branch required. This overrides the general 'never push to main' rule for this specific personal firmware config repo. The existing commit history also shows direct-to-main workflow.

<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019e885d-0f18-76fa-adda-9c4ddbe648b2 -->
* **ZMK Corne base layer layout: leftmost column and thumb cluster assignments**: ZMK Corne base layer layout (colemak\_layer + qwerty\_layer): Top-left: \`\&kp TAB\` (plain, no layer hold). Top-right: \`\&kp ESC\` (plain, no hold). Mid-right: \`\&ht RSHFT SQT\` (tap \`'\` / hold Right Shift — symmetric with left \`\&ht LSHFT BSPC\`). Bottom-right: \`\&ht LC(LALT) RBKT\` (tap \`]\` / hold Ctrl+Alt). Left thumb (inner→middle→outer): \`\&lt\_tog ADJUST ADJUST\` / \`\&mt LALT SPACE\` / \`\&mt LCTRL DEL\`. Right thumb: \`\&ht RCTRL RET\` / \`\&kp LGUI\` / \`\&lt\_tog RAISE RAISE\`. Layer structure: 0=COLEMAK, 1=QWERTY, 2=ADJUST (F-keys/BT/RGB/bootloader), 3=RAISE (numbers/nav/media). \`conditional\_layers\` removed; ADJUST reached only via left-inner thumb.

### Pattern

<!-- lore:019e885d-0f0b-7557-9359-7bb6190da67d -->
* **ZMK Corne keymap: custom lt\_tog hold-tap for layer thumb keys**: ZMK Corne keymap: custom \`lt\_tog\` hold-tap for layer thumb keys. RAISE layer (3): top row = \`\` \` 1–0 - \`\`; right home row = \`HOME UP END \ . .\`; right bottom row = \`PGUP LFT DWN RGT PGDN .\`; left rows = media/volume. ADJUST layer (2, repurposed LOWER slot): F1–F12 (F12 top-left), BT/RGB/layout-switch/bootloader (BOOT top-right). Left-inner thumb: \`\&lt\_tog ADJUST ADJUST\`. Right thumb (inner→middle→outer): \`\&ht RCTRL RET\` / \`\&kp LGUI\` / \`\&lt\_tog RAISE RAISE\`. Mid-right: \`\&ht RSHFT SQT\` (tap \`'\` / hold Right Shift). Bottom-right: \`\&ht LC(LALT) RBKT\` (tap \`]\` / hold Ctrl+Alt). Top-left: plain \`\&kp TAB\`. Top-right: plain \`\&kp ESC\`. Word movement: hold \`\&mt LCTRL DEL\` + RAISE simultaneously (Ctrl+arrows on RAISE layer).

### Preference

<!-- lore:019e854a-623a-77b9-a5e1-a672f1cd025f -->
* **zmk-config-corneview: commit directly to main (user-directed)**: For the zmk-config-corneview repo, Burak Yigit Kaya explicitly directs committing and pushing directly to \`main\` — no PR/branch required. This overrides the general 'never push to main' rule for this specific personal firmware config repo. The existing commit history also shows direct-to-main workflow.
